<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Official Missoula Mike Snow Report</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
      :root {
        --bg: #050816;
        --card: #101524;
        --accent: #6ce0ff;
        --accent-soft: rgba(108, 224, 255, 0.12);
        --text: #f9fafb;
        --muted: #9ca3af;
        --badge: #22c55e;
        --warning: #f97316;
      }

      * {
        box-sizing: border-box;
      }

      body {
        margin: 0;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
        font-family: system-ui, -apple-system, BlinkMacSystemFont, "SF Pro Text", sans-serif;
        background:
          radial-gradient(circle at top, #1f2937 0, #020617 52%),
          radial-gradient(circle at bottom, #0f172a 0, #000 55%);
        color: var(--text);
      }

      .card {
        position: relative;
        width: min(480px, 100%);
        padding: 32px 26px 26px;
        border-radius: 26px;
        background:
          radial-gradient(circle at top left, rgba(108, 224, 255, 0.35), transparent 55%),
          radial-gradient(circle at bottom right, rgba(249, 115, 22, 0.25), transparent 60%),
          var(--card);
        border: 1px solid rgba(148, 163, 184, 0.7);
        box-shadow:
          0 22px 55px rgba(15, 23, 42, 0.95),
          0 0 0 1px rgba(15, 23, 42, 1);
        outline: 1px solid rgba(236, 252, 203, 0.45);
        outline-offset: 3px;
        overflow: hidden;
        transform-origin: center;
        animation: cardFloat 4s ease-in-out infinite alternate;
      }

      .badge {
        display: inline-flex;
        align-items: center;
        gap: 6px;
        padding: 4px 10px;
        border-radius: 999px;
        font-size: 11px;
        letter-spacing: 0.08em;
        text-transform: uppercase;
        background: rgba(22, 163, 74, 0.12);
        border: 1px solid rgba(34, 197, 94, 0.6);
        color: #bbf7d0;
      }

      .badge-dot {
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: #22c55e;
        box-shadow: 0 0 10px #22c55e;
      }

      h1 {
        margin: 14px 0 4px;
        font-size: clamp(1.85rem, 2.4vw, 2.2rem);
        letter-spacing: 0.04em;
      }

      .subtitle {
        margin: 0 0 18px;
        color: var(--muted);
        font-size: 0.9rem;
      }

      .subtitle strong {
        color: var(--accent);
      }

      .grid {
        display: grid;
        grid-template-columns: minmax(0, 3fr) minmax(0, 2.4fr);
        gap: 20px;
        margin-top: 12px;
      }

      .section-title {
        font-size: 0.8rem;
        text-transform: uppercase;
        letter-spacing: 0.14em;
        color: var(--muted);
        margin-bottom: 10px;
      }

      .list {
        list-style: none;
        padding: 0;
        margin: 0;
        display: grid;
        gap: 8px;
        font-size: 0.9rem;
      }

      .list li {
        display: flex;
        gap: 10px;
        padding: 9px 10px;
        border-radius: 999px;
        background: radial-gradient(circle at left, rgba(108, 224, 255, 0.18), transparent 55%);
        border: 1px solid rgba(148, 163, 184, 0.45);
        align-items: center;
      }

      .list li span.icon {
        width: 18px;
        text-align: center;
      }

      .list li span.text-main {
        flex: 1;
      }

      .list li span.text-sub {
        font-size: 0.78rem;
        color: var(--muted);
      }

      .stamp {
        position: relative;
        border-radius: 20px;
        padding: 14px 14px 16px;
        border: 1px dashed rgba(148, 163, 184, 0.6);
        background:
          radial-gradient(circle at top left, rgba(108, 224, 255, 0.24), transparent 55%),
          radial-gradient(circle at bottom right, rgba(249, 115, 22, 0.17), transparent 55%),
          rgba(15, 23, 42, 0.95);
      }

      .stamp-title {
        font-size: 0.75rem;
        text-transform: uppercase;
        letter-spacing: 0.18em;
        color: var(--muted);
        margin-bottom: 8px;
      }

      .stamp-main {
        font-size: 1.6rem;
        font-weight: 700;
        letter-spacing: 0.08em;
        text-transform: uppercase;
      }

      .stamp-main span.accent {
        color: var(--accent);
        text-shadow: 0 0 12px rgba(108, 224, 255, 0.8);
      }

      .stamp-sub {
        font-size: 0.8rem;
        color: var(--muted);
        margin-top: 6px;
      }

      .stamp-footer {
        margin-top: 10px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 0.74rem;
        color: var(--muted);
      }

      .stamp-tag {
        padding: 3px 8px;
        border-radius: 999px;
        border: 1px solid rgba(249, 115, 22, 0.7);
        color: #fed7aa;
        display: inline-flex;
        align-items: center;
        gap: 6px;
      }

      .warning-dot {
        width: 7px;
        height: 7px;
        border-radius: 999px;
        background: var(--warning);
        box-shadow: 0 0 10px var(--warning);
      }

      .footer-note {
        margin-top: 18px;
        font-size: 0.78rem;
        color: var(--muted);
        display: flex;
        align-items: center;
        gap: 8px;
      }

      .footer-note strong {
        color: var(--accent);
      }

      .footer-note span.code {
        font-family: "JetBrains Mono", ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
        font-size: 0.75rem;
        padding: 2px 6px;
        border-radius: 6px;
        background: rgba(15, 23, 42, 0.9);
        border: 1px solid rgba(148, 163, 184, 0.4);
      }

      .skyline {
        position: absolute;
        inset: auto -40px 0;
        height: 80px;
        pointer-events: none;
        opacity: 0.45;
        background:
          radial-gradient(circle at 12% 0, rgba(148, 163, 184, 0.4) 0, transparent 55%),
          linear-gradient(to top, rgba(15, 23, 42, 1), transparent 55%);
        mask-image: linear-gradient(to top, black 0, transparent 60%);
      }

      .mountains {
        position: absolute;
        inset: auto 0 -14px;
        height: 80px;
        background:
          radial-gradient(circle at 10% 130%, rgba(30, 64, 175, 0.9) 0, transparent 55%),
          radial-gradient(circle at 55% 130%, rgba(15, 118, 110, 0.9) 0, transparent 55%),
          radial-gradient(circle at 88% 130%, rgba(30, 64, 175, 0.9) 0, transparent 55%);
        filter: blur(2px);
        opacity: 0.95;
        mix-blend-mode: screen;
      }

      .moon {
        position: absolute;
        top: 14px;
        right: 24px;
        width: 52px;
        height: 52px;
        border-radius: 999px;
        background: radial-gradient(circle at 30% 25%, #fefce8 0, #facc15 45%, #f97316 90%);
        box-shadow:
          0 0 22px rgba(250, 204, 21, 0.9),
          0 0 60px rgba(56, 189, 248, 0.8);
        border: 1px solid rgba(250, 250, 249, 0.5);
      }

      .moon::after {
        content: "🌙";
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        font-size: 24px;
      }

      @keyframes cardFloat {
        0% {
          transform: translateY(0px) rotate(-0.4deg);
        }
        100% {
          transform: translateY(-6px) rotate(0.4deg);
        }
      }

      @media (max-width: 640px) {
        body {
          padding: 12px;
        }

        .card {
          padding: 18px 14px 16px;
          border-radius: 16px;
        }

        .grid {
          grid-template-columns: minmax(0, 1fr);
          gap: 16px;
        }

        .moon {
          width: 44px;
          height: 44px;
          top: 10px;
          right: 14px;
        }

        h1 {
          font-size: 1.5rem;
          line-height: 1.2;
        }

        .subtitle {
          font-size: 0.85rem;
          line-height: 1.5;
        }

        .list {
          font-size: 0.85rem;
        }

        .stamp-main {
          font-size: 1.3rem;
        }

        .footer-note {
          flex-direction: column;
          align-items: flex-start;
        }
      }

        .card {
          padding: 20px 16px 18px;
          border-radius: 18px;
        }

        .grid {
          grid-template-columns: minmax(0, 1fr);
        }

        .moon {
          width: 44px;
          height: 44px;
          top: 12px;
          right: 16px;
        }

        h1 {
          font-size: 1.65rem;
        }
      }
    </style>
  </head>
  <body>
    <main class="card" aria-labelledby="title">
      <div class="moon" aria-hidden="true"></div>

      <div class="badge">
        <span class="badge-dot"></span>
        Live Transmission  •  Missoula Sector
      </div>

      <h1 id="title">Missoula Mike: Local Snowboard Icon Report</h1>
      <p class="subtitle">
        This extremely serious and highly official document confirms that <strong>Mike</strong> is out
        in <strong>Missoula, Montana</strong> snowboarding, drinking coffee, and generally being a
        very good human. We are simply here to document how iconic that is.
      </p>

      <section class="grid" aria-label="Mike dossier">
        <section aria-label="Primary diagnostics">
          <h2 class="section-title">Field Notes</h2>
          <ul class="list">
          <li>
            <span class="icon">🏂</span>
            <span class="text-main">Has permanent "having the best day" energy on a snowboard.</span>
          </li>
          <li>
            <span class="icon">🍪</span>
            <span class="text-main">Once said "i had cookie" and then spent 40 minutes talking about how peaceful the mountains feel. No notes.</span>
          </li>
          <li>
            <span class="icon">❄️</span>
            <span class="text-main">Knows a ridiculous amount about snow and appreciates every single flake.</span>
          </li>
          <li>
            <span class="icon">📱</span>
            <span class="text-main">Reply speed: not instant, but always kind, always real.</span>
          </li>
        </ul>
        </section>

        <aside aria-label="Official stamp of chaos" class="stamp">
          <div class="stamp-title">Certified By The Official Friends of Mike Appreciation Society</div>
          <div class="stamp-main">
            <span class="accent">MIKE</span>
            <span>• 100% LEGEND</span>
          </div>
          <p class="stamp-sub">
            Approved for unlimited runs in Missoula, as long as he occasionally sends "look how pretty it is"
            photos and remembers that his friends think he is kind of the best.
          </p>
          <div class="stamp-footer">
            <span>Rider ID: MSL-406-MIKE</span>
            <span class="stamp-tag">
              <span class="warning-dot"></span>
              Heart of gold, vibes off the charts
            </span>
          </div>
        </aside>
      </section>

      <p class="footer-note">
        <span>Reminder:</span>
        <span>
          This page is basically a tiny love letter to Mike. Visiting this site officially counts as
          hanging out with your friend, and any attempt to explain it away as just "i had cookie" will
          be politely overruled.
        </span>
        <span class="code">friendship.html</span>
      </p>

      <div class="skyline" aria-hidden="true"></div>
      <div class="mountains" aria-hidden="true"></div>
    </main>
  </body>
</html>
